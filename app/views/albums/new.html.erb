<div class="l-box">
  <h1>New album</h1>

  <% if @images.any? %>
    <p>
      The following images will be added to this new album. You can change the
      image ordering, add/remove images after the album has been created.
    </p>
    <%= render 'images/tiles', :images => @images %>
  <% end %>
</div>

<div class="l-box">
  <%= render 'albums/form' %>
</div>
