require 'spec_helper'

include UploadHelper, UserFactory

describe 'user profile header', :type => :feature do
  it "shows the user's name" do
    pending "Don't even have a username field in DB yet."
  end
end